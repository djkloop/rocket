export enum ESchemaType {
  UISCHEMA = "uiSchema",
  DATASCHEMA = "dataSchema",
  DATA = "data",
  UNITEDSCHEMA = "unitedSchema",
}

export enum ESetSchemaType {
  DATA = "data",
  UISCHEMA = "uiSchema",
  DATASCHEMA = "dataSchema",
}
